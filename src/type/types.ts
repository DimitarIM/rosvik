export type MainHeroSectionProps = {
    firstText: string
    secondText: string
    thirdText: string
}

export type DownloadButtonProps = {
    image: string
    alt: string
}

export type LogoProps = {
    image: string
    alt: string
}