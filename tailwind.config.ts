import type { Config } from "tailwindcss"

const config: Config = {
  
}
module.exports = {
  theme: {
    extend: {
        fontFamily:{
            norse: ['var(--font-norse)']
        }
    }
  },

}